# Pendientes

Tareas opcionales fuera del alcance actual de entrega.

## Estado produccion

Con Supabase configurado (`.env` + Docker + sesion activa), la app usa datos reales. Los mocks solo aplican si `SUPABASE_URL` no esta definido (desarrollo sin backend).

## Mejoras futuras

- [ ] Notificaciones push (FCM/APNs) — el inbox persistente en tabla `notificacion` ya existe; falta envio al dispositivo.
- [ ] Campanas publicitarias pagadas — la consola admin muestra metricas organicas; no hay tabla de ads aun.
